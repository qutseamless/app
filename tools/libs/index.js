const bundle = require('./bundle');
const clean = require('./clean');
const copy = require('./copy');
const dir = require('./dir');
const run = require('./run');

module.exports = { bundle, clean, copy, dir, run };
